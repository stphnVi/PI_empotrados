<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/intellihome"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiPrimerAplicacion"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".Registro"
            android:exported="false" />
        <activity
            android:name=".SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.MiPrimerAplicacion" />
        <activity
            android:name=".ui.login.SignUpActivity"
            android:exported="false"
            android:label="@string/title_activity_sign_up" /> <!-- Esta es la configuración de osmdroid -->
        <meta-data
            android:name="org.osmdroid.directions.ODDirectionService"
            android:value="true" />
        <meta-data
            android:name="org.osmdroid.tileprovider.MapTileProviderBase"
            android:value="org.osmdroid.tileprovider.tilesource.TileSourceFactory.MAPNIK" /> <!-- Declarar la actividad ExitActivity -->
        <activity android:name=".RegistroActivity" /> <!-- Declarar la actividad registro_casa -->
        <activity android:name=".RegistroCasa" /> <!-- Declarar la actividad Principal -->
        <activity android:name=".PrincipalActivity" /> <!-- Declarar la actividad registro_casa -->
        <activity android:name=".Monitoreo" /> <!-- Declarar la actividad Casa modelo -->
        <activity android:name=".CasaModelo" /> <!-- Declarar la actividad Recuperacion de contraseña -->
        <activity android:name=".RecuperacionContrasena" /> <!-- Declarar la actividad BusquedaAlquiler -->
        <activity android:name=".BusquedaAlquiler" /> <!-- Declarar la actividad ResultadosBusqueda -->
        <activity android:name=".ResultadosBusqueda" /> <!-- Declarar la actividad OpcionSeleccionada -->
        <activity android:name=".OpcionSeleccionada" /> <!-- Declarar la actividad AlquilerPropiedad -->
        <activity android:name=".AlquilerPropiedad" /> <!-- Declarar la actividad MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PreguntasActivity" />
    </application>

</manifest>